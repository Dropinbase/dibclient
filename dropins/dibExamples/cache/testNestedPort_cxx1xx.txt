(function() {
  var require = ['Ext.ux.form.DibFieldSet','Ext.img','Dropins.setSencha.dibGlobals.js.app.components.DibTabPanel' ];
  require.push('/dropins/setNgMaterial/dibGlobals/js/component/selectDefault.js');
  var files = [];
  angular.forEach(require, function (file,index) { 
    files.push(DIB__base_url+'/files'+file);
  });
    var testNestedPort = angular.module('testNestedPort', [files]).run(['$rootScope', 'actionLoaderService', function($rootScope, actionLoaderService) {
            var actions = ['dibGlobals.action.OpenContainer','dibGlobals.action.ToggleMenu'];
            if (!actions || actions.length === 0) {
                return;
            }
            actionLoaderService.loadDibActionDefinitions(actions);
    }]);
})();
(function() {
var testNestedPort = angular.module('testNestedPort');
testNestedPort.component('testNestedPort', {
    controllerAs: 'testNestedPort',
    templateUrl: DIB__base_url + '/dropins/setNgMaterial/dibContainer/Template/view/testNestedPort.html',
    controller : component__testNestedPort
});
component__testNestedPort.$inject = [  
            '$scope', '$ocLazyLoad', '$rootScope', '$log', '$q','containerService','recordFactory','crudService','utilityService'];
    function component__testNestedPort($scope, $ocLazyLoad, $rootScope, $log, $q,containerService,recordFactory,crudService,utilityService) {
                     $scope.container = {};
            $scope.container.Id = '6799';
            $scope.container.Name = 'testNestedPort';
            $("[dib-container='testNestedPort']").trigger('load');
            var crudController = "/peff/Crud";
                    $scope.model = {};
        $scope.model._GET = recordFactory.modelConstruct();
        $scope.model._GET['imagestatic'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['htmllabel'] = containerService.modelItem("text", 
            false,
            false
        );
            var reloadContainerFunctions = [];
            containerService.watchModel($scope);
	        $scope.common = $rootScope.common;
            $scope.primaryKeys = [];
        $scope.menus = {};
        $scope.dropdowns = {};
        //load all dropdowns required for this container
            $scope.openAuditTrail = function (portAlias) {
                $log.warn('Audit trail not available for this container, as a table is not linked to this view');
            };
        $scope.reloadContainer = function () {
            //Now we reload all the required 
            containerService.reloadChildren({containerName: 'testNestedPort'});
        };
        if (!!containerService.isSubContainer($("[dib-container=testNestedPort]")) === false) { 
            $scope.reloadContainer();
        }
    } 
})();
