(function() {
  var require = ['/dropins/setNgMaterial/dibGlobals/js/component/timeDatePickerInput.js' ];
  require.push('/dropins/setNgMaterial/dibGlobals/js/component/selectDefault.js');
  var files = [];
  angular.forEach(require, function (file,index) { 
    files.push(DIB__base_url+'/files'+file);
  });
    var dibtestdibContainer = angular.module('dibtestdibContainer', [files]);
    function updateQueryStringMenu(url, node) { 
        if (!!node === true) {
            return url + "&node="+node;
        } 
        return url + "&node=root";
    };
    function updateQueryStringDropdown(url, query, modelType, page) {
        url += "&limit=20&page=" + (!!page == true? page: 1);
        if (modelType=='search') { 
            url += "&showUsedOnly=true";
        }
        url += "&query=";
        if (!!query == true) {
            url += query;
        }
        return url;
    };
})();
(function() {
var dibtestdibContainer = angular.module('dibtestdibContainer');
dibtestdibContainer.component('dibtestdibContainer', {
    controllerAs: 'dibtestdibContainer',
    templateUrl: DIB__base_url + '/dropins/setNgMaterial/dibContainer/Template/view/dibtestdibContainer.html',
    controller : component__dibtestdibContainer
});
component__dibtestdibContainer.$inject = [  
            '$scope', '$ocLazyLoad', '$rootScope', '$log', '$q','containerService','recordFactory','crudService','utilityService'];
    function component__dibtestdibContainer($scope, $ocLazyLoad, $rootScope, $log, $q,containerService,recordFactory,crudService,utilityService) {
        var vm = this;
                     $scope.container = {};
            $scope.container.Id = '7163';
            $scope.container.Name = 'dibtestdibContainer';
            var crudController = "/peff/Crud";
                    $scope.model = {};
        $scope.model._GET = recordFactory.modelConstruct();
        $scope.model._GET['textfield'] = containerService.modelItem("text", 
            true,
            true,
            null
        );
        $scope.model._GET['checkboxfield'] = containerService.modelItem("boolean", 
            true,
            true,
            null
        );
        $scope.model._GET['datefield'] = containerService.modelItem("date", 
            true,
            true,
            null
        );
        $scope.view = {};
            var reloadContainerFunctions = [];
            containerService.watchModel($scope);
	        $scope.common = $rootScope.common;
            $scope.primaryKeys = [];
        $scope.menus = {};
        $scope.dropdowns = {};
        //load all dropdowns required for this container
            $scope.openAuditTrail = function (portAlias) {
                $log.warn('Audit trail not available for this container, as a table is not linked to this view');
            };
        //required for permissions
        vm.$postLink = function () {
            $("[dib-container=dibtestdibContainer]").trigger('onInit');
        };
        vm.$onInit = function () { 
            $scope.recordStatus = "create";
        };
        $scope.reloadContainer = function () {
            //Now we reload all the required 
            containerService.reloadChildren({containerName: 'dibtestdibContainer'});
        };
        if (!!containerService.isSubContainer($("[dib-container=dibtestdibContainer]")) === false) { 
            $scope.reloadContainer();
        }
    } 
})();
