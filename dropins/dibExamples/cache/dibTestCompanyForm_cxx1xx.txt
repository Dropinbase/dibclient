(function() {
  var require = ['/dropins/setNgMaterial/dibForm/js/component/formPaging.js','/dropins/setNgMaterial/dibGlobals/js/component/taScrollWindow.js',  
		'/dropins/setNgMaterial/dibForm/js/services/dibForm.js'
	];
  require.push('/dropins/setNgMaterial/dibGlobals/js/component/selectDefault.js');
  var files = [];
  angular.forEach(require, function (file,index) { 
    files.push(DIB__base_url+'/files'+file);
  });
          files.push( DIB__base_url + '/dropins/setNgMaterial/dibGrid/Template/controller/dibtestCompanyConsultantGrid.js');
          files.push( DIB__base_url + '/dropins/setNgMaterial/dibGrid/Template/controller/dibtestConsultantGrid.js');
    var dibtestCompanyForm = angular.module('dibtestCompanyForm', [files]);
    function updateQueryStringMenu(url, node) { 
        if (!!node === true) {
            return url + "&node="+node;
        } 
        return url + "&node=root";
    };
    dibtestCompanyForm.service('service173713',service173713); 
    service173713.$inject = ['ajax', 'discoverService', '$q'];
    function service173713(ajax, discoverService, $q) {
            return {
                list : list
            };       
            function list(query, modelType, page) {
                var $defer = $q.defer();
                var url = updateQueryStringDropdown(DIB__base_url + "/peff/Crud/componentlist/dibtestCompanyForm?containerItemId=173713", query, modelType, page);
                var requestInfo = discoverService.addToUrl({
                    field :$('#ci173713'),
                    container : $('[dib-container=dibtestCompanyForm]'),
                    url : url
                });
                return ajax.post(requestInfo, {
                    excludeFromCancels : true,
                    factoryResult : function (data) {
                        return data.records;
                    }
                });
            };
    };
    dibtestCompanyForm.service('service173716',service173716); 
    service173716.$inject = ['ajax', 'discoverService', '$q'];
    function service173716(ajax, discoverService, $q) {
            return {
                list : list
            };       
            function list(query, modelType, page) {
                var $defer = $q.defer();
                var url = updateQueryStringDropdown(DIB__base_url + "/peff/Crud/componentlist/dibtestCompanyForm?containerItemId=173716", query, modelType, page);
                var requestInfo = discoverService.addToUrl({
                    field :$('#ci173716'),
                    container : $('[dib-container=dibtestCompanyForm]'),
                    url : url
                });
                return ajax.post(requestInfo, {
                    excludeFromCancels : true,
                    factoryResult : function (data) {
                        return data.records;
                    }
                });
            };
    };
    function updateQueryStringDropdown(url, query, modelType, page) {
        url += "&limit=20&page=" + (!!page == true? page: 1);
        if (modelType=='search') { 
            url += "&showUsedOnly=true";
        }
        url += "&query=";
        if (!!query == true) {
            url += query;
        }
        return url;
    };
var dibtestCompanyForm = angular.module('dibtestCompanyForm');
dibtestCompanyForm.component('dibtestCompanyForm', {
    controllerAs: 'dibtestCompanyForm',
    templateUrl: DIB__base_url + '/dropins/setNgMaterial/dibForm/Template/view/dibtestCompanyForm.html',
    controller : component__dibtestCompanyForm
});
component__dibtestCompanyForm.$inject = [  
            'dibForm', '$scope','$element', '$ocLazyLoad', '$rootScope', '$log', 'discoverService', 'messageService','$location','actionService', '$q','utilityService', '$timeout', 'activeFilterOverride','service173713','service173716','containerService','recordFactory','crudService','utilityService'];
    function component__dibtestCompanyForm(dibForm, $scope,$element, $ocLazyLoad, $rootScope, $log, discoverService, messageService,$location,actionService, $q,utilityService, $timeout, activeFilterOverride,service173713,service173716,containerService,recordFactory,crudService,utilityService) {
        var ctrl = this;        
        ctrl.$postLink = function () {
        if (!!$scope) { 
                $scope.$broadcast('postLink');
        }
        };
        ctrl.$onInit = function () {
        if (!!$scope) { 
                $scope.$broadcast('onInit');
        }
                        $scope.container = {};
            $scope.container.Id = '7961';
            $scope.container.Name = 'dibtestCompanyForm';
            var crudController = "/peff/Crud";
                    $scope.model = {};
        $scope.model._GET = recordFactory.modelConstruct();
        $scope.model._GET['id'] = containerService.modelItem("number", 
            false,
            false,
            null
        );
        $scope.model._GET['name'] = containerService.modelItem("text", 
            true,
            true,
            null
        );
        $scope.model._GET['chinese_name'] = containerService.modelItem("text", 
            true,
            true,
            null
        );
        $scope.model._GET['parent_company_id'] = containerService.modelItem("select", 
            true,
            true,
            service173713
        );
        $scope.model._GET['notes'] = containerService.modelItem("text", 
            true,
            true,
            null
        );
        $scope.model._GET['website'] = containerService.modelItem("text", 
            true,
            true,
            null
        );
        $scope.model._GET['parent_company_contact_id'] = containerService.modelItem("select", 
            true,
            true,
            service173716
        );
        $scope.model._GET['icon'] = containerService.modelItem("text", 
            true,
            true,
            null
        );
        $scope.view = {};
            $scope.view.id = {'disabled' : false,'visible' : true, 'class' : null, 'style' : null };
            var reloadContainerFunctions = [];
            containerService.watchModel($scope);
	        $scope.common = $rootScope.common;
            $scope.primaryKeys = ["id"];
        $scope.menus = {};
        $scope.dropdowns = {};
        //load all dropdowns required for this container
            $scope.dropdowns.service173713 = {};
            $scope.dropdowns.service173713.items = [];
            var setTimeoutCancel173713 = null;
            var query173713;
            $scope.loadService173713 = function (query, skipTimeout) {
                //This is to prevent to load any unnessary data as this gets triggered a few times with the multi line editing
                if (query173713 != query) {
                    query173713 = query;
                    if (!!skipTimeout) {
                        return service173713.list(query).then(function (items) {
                            $scope.dropdowns.service173713.items = items;
                        });
                    } else {
                        clearTimeout(setTimeoutCancel173713);
                        setTimeoutCancel173713 = setTimeout(function () { 
                            service173713.list(query).then(function (items) {
                                $scope.dropdowns.service173713.items = items;
                            });
                        }, 100);
                    }
                } 
            };
            $scope.dropdowns.service173713.searchTextChange = function (query) {
                $scope.loadService173713(query);
            };
            $scope.dropdowns.service173713.getItems = function (query,modelType) {
                //return promise this will asign the result to the items
                return service173713.list(query,modelType);
            };
            $scope.select173713 = function (model,modelName,fieldName) {
                if (!!modelName == false) modelName ="model";
                var $element =  $('#ci173713');
                if (!!$element.attr('select-model') === false) { 
                    $element = $element.find('[select-model]');
                }
                var modelName =  'selected___' + fieldName;
                $element.trigger('select');
                if (!!model[modelName]  === false) { 
                    return  false;
                }
                angular.forEach($scope.dropdowns.service173713.items, function (item, index)   {
                    if (item.id == model[modelName]['id']) {
                        model[modelName]['id_display_value'] = item.id_display_value; 
                        return false;
                    }
                });
            };
            var setTimeout173713 = null;
            //loadService173713();
            //reloadContainerFunctions.push($scope.loadService173713);
            $scope.dropdowns.service173716 = {};
            $scope.dropdowns.service173716.items = [];
            var setTimeoutCancel173716 = null;
            var query173716;
            $scope.loadService173716 = function (query, skipTimeout) {
                //This is to prevent to load any unnessary data as this gets triggered a few times with the multi line editing
                if (query173716 != query) {
                    query173716 = query;
                    if (!!skipTimeout) {
                        return service173716.list(query).then(function (items) {
                            $scope.dropdowns.service173716.items = items;
                        });
                    } else {
                        clearTimeout(setTimeoutCancel173716);
                        setTimeoutCancel173716 = setTimeout(function () { 
                            service173716.list(query).then(function (items) {
                                $scope.dropdowns.service173716.items = items;
                            });
                        }, 100);
                    }
                } 
            };
            $scope.dropdowns.service173716.searchTextChange = function (query) {
                $scope.loadService173716(query);
            };
            $scope.dropdowns.service173716.getItems = function (query,modelType) {
                //return promise this will asign the result to the items
                return service173716.list(query,modelType);
            };
            $scope.select173716 = function (model,modelName,fieldName) {
                if (!!modelName == false) modelName ="model";
                var $element =  $('#ci173716');
                if (!!$element.attr('select-model') === false) { 
                    $element = $element.find('[select-model]');
                }
                var modelName =  'selected___' + fieldName;
                $element.trigger('select');
                if (!!model[modelName]  === false) { 
                    return  false;
                }
                angular.forEach($scope.dropdowns.service173716.items, function (item, index)   {
                    if (item.id == model[modelName]['id']) {
                        model[modelName]['id_display_value'] = item.id_display_value; 
                        return false;
                    }
                });
            };
            var setTimeout173716 = null;
            //loadService173716();
            //reloadContainerFunctions.push($scope.loadService173716);
            $scope.openAuditTrail = function (portAlias) {
                            var primaryKeyData = recordFactory.primaryKeyAuditIds($scope.primaryKeys, $scope.model);
                            if (primaryKeyData.length > 0) {
                            $scope.common.goTo(DIB__audit_trail_container,portAlias,true,"?filter_pef_table_id=1440&filter_record_id="+primaryKeyData);
                        } else {
                            $scope.common.goTo(DIB__audit_trail_container,portAlias,true,"?filter_pef_table_id=1440");
                        }
            };
            $scope.addModel = function () { 
                dibForm.addModel($scope, "dibtestCompanyForm");
            };
            $scope.deleteRecord = function (options) {
                dibForm.deleteRecord('dibtestCompanyForm', $scope, $scope.dibtestCompanyForm, options, crudController).then(function (){
        if (!!$scope) { 
                $scope.$broadcast('delete');
        }
                });
            };         
            /**
            *  Saving record in this model
            */
            $scope.saveModel = function (options) {
                if ($scope.loadingData) return $q.reject('Busy loading...');
                if (dibForm.validate($scope, $scope.dibtestCompanyForm)) {
        if (!!$scope) { 
                $scope.$broadcast('beforeUpdate');
        }
                    $scope.loadingData =  true;
                    var primaryKeyData = recordFactory.primaryKeyData($scope.primaryKeys, $scope.model);
                    return dibForm.updateRecord('dibtestCompanyForm', $scope, $scope.dibtestCompanyForm, crudController, options).then(function () { 
                        if (Object.keys(primaryKeyData).length>0) {
        if (!!$scope) { 
                $scope.$broadcast('update');
        }
                        } else {
        if (!!$scope) { 
                $scope.$broadcast('create');
        }
                            loadForm();
                        }
                        return $q.when();
                    });
                }
            };
            var mdTabs = $("[dib-container=dibtestCompanyForm]").closest('md-tabs');
            var tabsScope = null;
            if (mdTabs.length === 0) {
                mdTabs = null;
            }
            else {
                tabsScope = mdTabs.scope();
            }
            $scope.$watch('dibtestCompanyForm.$dirty', function (newValue, oldValue) { 
                $rootScope._navigation.containerDirty = newValue;
                if ($rootScope._navigation.containerDirty && !!mdTabs && !!tabsScope) {
                    var currentIndex = tabsScope.selectedIndex;
                    $rootScope._navigation.addSaveFailedPromise(function(tabIndex) {
                        tabsScope.selectedIndex = tabIndex;
                        return $q.when();
                    }, [currentIndex]);
                }
            },true);
            $rootScope._navigation.addSavePromise($scope.saveModel);
            //Giving the form the ability to handle dependency data (if an event is trigger on another component that has a refresh item alias point to the form/child form)
            $scope.setDependencyData = function (object) { 
                return dibForm.setDependencyData($scope,object);
            };
            $scope.itemAliasData = function () {
                return angular.copy($scope.primaryKeyData);
            };
            /**
            * Clear the form from records and dependancy data
            */
            $scope.clearForm = function () {
                if ($scope.loadingData) return;
                $scope.primaryKeyData = null;
                $scope.reloadContainer();
                dibForm.clean($scope.dibtestCompanyForm,$scope.model);
        if (!!$scope) { 
                $scope.$broadcast('clearDependency');
        }
            };
            function loadForm () { 
                $rootScope._navigation.reset();
                $rootScope._navigation.addSavePromise($scope.saveModel);
        if (!!$scope) { 
                $scope.$broadcast('beforeLoad');
        }
                //clear the record status
                $scope.recordStatus = null;
                var isUrlContainer = containerService.urlContainer('dibtestCompanyForm');
                if (isUrlContainer) {
                    //check if record is first or last
                    if (!!$location.search()['record'] && $location.search()['record']!='new') {
                        return;
                    }
                }
                //check if this is a child container 
                $scope.primaryKeyData = isUrlContainer ? discoverService.getPrimaryKeysFromUrl() : $scope.primaryKeyData;
                if (!!$scope.primaryKeyData === false) {
                $scope.primaryKeyData = {};
                //set all the primaryKeys to 0
                angular.forEach($scope.primaryKeys, function (key,index) { 
                    $scope.model[key] = 0;
                });
                }
                //remove active filter if no record are available
                if (Object.keys($scope.primaryKeyData).length ==0) {
                    $scope.recordStatus = "create";
                } else {
                    $scope.recordStatus = "update";
                }
                $scope.loadingData = true;
                $scope.currentPrimaryKeyData = $scope.primaryKeyData;
                crudService.read(crudController,'dibtestCompanyForm',{
                    primaryKeyData : $scope.currentPrimaryKeyData,
                    createParams: "{}"
                },
                $scope
                ).then(function (model) {
                    $scope.model = model;
                    $scope.currentPrimaryKeyData = recordFactory.primaryKeyData($scope.primaryKeys, $scope.model);
        if (!!$scope) { 
                $scope.$broadcast('load');
        }
                    $scope.dibtestCompanyForm.$setUntouched();
                    $scope.dibtestCompanyForm.$setPristine();
                    //@TODO refactor start 
                    var timeoutTest = $timeout(function () { 
                        containerService.reloadChildren({containerName: 'dibtestCompanyForm'});
                    },500); 
                    //@FRANCOIS @TODO Maybe we can change this to a better magic number :)
                    var eventDeregister = $rootScope.$on("dibAngular.subcontainerLoaded", function () {
                        $timeout.cancel(timeoutTest);
                        timeoutTest = $timeout(function () { 
                            containerService.reloadChildren({containerName: 'dibtestCompanyForm'});
                            eventDeregister();
                        },500); 
                        //@FRANCOIS @TODO Maybe we can change this to a better magic number :)
                    });
                    //Refactor stop
                    $log.debug("containerName read->model",$scope.model );
                    $scope.loadingData =  false;
                }, function () {
                    $scope.loadingData =  false;
                });
            }
            $scope.reloadContainer = function (options) {
        if (!!$scope) { 
                $scope.$broadcast('beforeContainerReload');
        }
                $scope.autoRefresh = true;
                loadForm ();
        if (!!$scope) { 
                $scope.$broadcast('reloadContainer');
        }
            };
            //var isUrlContainer = containerService.urlContainer('dibtestCompanyForm');
            //the if below we removed the requirement to match the url to the containerName, so we can load data into a window without the url changing. wizBuildAppAdv.
            loadForm();
            $scope.$watch('model', function (oldVal, newVal) {
        if (!!$scope) { 
                $scope.$broadcast('modelChange');
        }
            },true);  
        }
    }
})();
