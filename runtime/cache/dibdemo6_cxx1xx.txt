(function() {
  var require = [ ];
  require.push('/dropins/setNgMaterial/dibGlobals/js/component/selectDefault.js');
  var files = [];
  angular.forEach(require, function (file,index) { 
    files.push(DIB__base_url+'/files'+file);
  });
    var dibdemo6 = angular.module('dibdemo6', [files]).run(['$rootScope', 'actionLoaderService', function($rootScope, actionLoaderService) {
            var actions = ['dibGlobals.action.OpenContainer','dibGlobals.action.ToggleMenu'];
            if (!actions || actions.length === 0) {
                return;
            }
            actionLoaderService.loadDibActionDefinitions(actions);
    }]);
        dibdemo6.component('tabModule192617', {
                controllerAs: 'tabModule192617',
                controller : ["$scope", function ($scope) { 
                    //debugger;
                }]
            });
})();
(function() {
var dibdemo6 = angular.module('dibdemo6');
dibdemo6.component('dibdemo6', {
    controllerAs: 'dibdemo6',
    templateUrl: DIB__base_url + '/dropins/setNgMaterial/dibContainer/Template/view/dibdemo6.html',
    controller : component__dibdemo6
});
component__dibdemo6.$inject = [  
            '$scope', '$ocLazyLoad', '$rootScope', '$log', '$q','containerService','recordFactory','crudService','utilityService'];
    function component__dibdemo6($scope, $ocLazyLoad, $rootScope, $log, $q,containerService,recordFactory,crudService,utilityService) {
                     $scope.container = {};
            $scope.container.Id = '8715';
            $scope.container.Name = 'dibdemo6';
            $("[dib-container='dibdemo6']").trigger('load');
            var crudController = "/peff/Crud";
                    $scope.model = {};
        $scope.model._GET = recordFactory.modelConstruct();
        $scope.view = {};
            var reloadContainerFunctions = [];
            containerService.watchModel($scope);
	        $scope.common = $rootScope.common;
            $scope.primaryKeys = [];
        $scope.menus = {};
        $scope.dropdowns = {};
        //load all dropdowns required for this container
            $scope.openAuditTrail = function (portAlias) {
                $log.warn('Audit trail not available for this container, as a table is not linked to this view');
            };
        $scope.reloadContainer = function () {
            //Now we reload all the required 
            containerService.reloadChildren({containerName: 'dibdemo6'});
        };
        if (!!containerService.isSubContainer($("[dib-container=dibdemo6]")) === false) { 
            $scope.reloadContainer();
        }
    } 
})();
