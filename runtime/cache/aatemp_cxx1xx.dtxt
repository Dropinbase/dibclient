
(function() {
    var require = ['/dropins/setNgMaterial/dibMenu/js/components/dibSideBarStatus.js' ];
    require.push('/dropins/setNgMaterial/dibGlobals/js/component/selectDefault.js');
    var files = [];
    angular.forEach(require, function (file,index) { 
        files.push(DIB.base_url+'/files'+file);
    });
    var aatemp = angular.module('aatemp', [files]);
    aatemp.service('service209283',service209283);
    service209283.$inject = ['dibAjax', 'dibDiscover'];
    function service209283(dibAjax,  dibDiscover) {
            return {
                list : list
            };
            function list($scope, $element, node) {
                var url = dibDiscover.updateQueryStringMenu(DIB.base_url + "/dropins/setNgMaterial/dibMenu/menu/list.json?containerName=aatemp&rootMenuId=209284", node);
                var requestInfo = dibDiscover.addToUrl({
                    field : $('#ci209283'),
                    $scope : $scope,
                    container : $element,
                    url : url
                });
                return dibAjax.post(requestInfo, {
                    excludeFromCancels : true,
                    factoryResult : function (data) {
                        return data.records;
                    }
                })
            };
    };
    var aatemp = angular.module('aatemp');
    aatemp.component('aatemp', {
        controllerAs: 'aatemp',
        templateUrl: DIB.base_url + '/dropins/setNgMaterial/dibContainer/Template/view/aatemp.html',
        controller : component__aatemp
    });
    component__aatemp.$inject = [ '$scope','$element','dibCrud','dibNavigation','dibAction','dibContainer','dibDiscover','dibRecord','dibUtility','dibCommon','$log','$q','$ocLazyLoad','service209283'];
    function component__aatemp($scope,$element,dibCrud,dibNavigation,dibAction,dibContainer,dibDiscover,dibRecord,dibUtility,dibCommon,$log,$q,$ocLazyLoad,service209283) {
        var ctrl = this;
        ctrl.$onInit = function () { 
                        $scope.container = {};
            $scope.container.Id = '9074';
            $scope.container.Name = 'aatemp';
            $scope.container.SubContainerItemAliases = []; 
            var crudController = "/peff/Crud";
                    $scope.model = {};
        $scope.model._GET = dibRecord.modelConstruct($scope);
        $scope.view = {};
            $scope.view.menu = {'disabled' : false,'visible' : true, 'class' : null, 'style' : null };
            $scope.view.menuItems = {'disabled' : false,'visible' : true, 'class' : null, 'style' : null };
            var reloadContainerFunctions = [];
            dibContainer.watchModel($scope);
	        $scope.dibCommon = dibCommon;
            $scope.primaryKeys = [];
        var $dibContainer = $element.first();
        $scope.menus = {};
            $scope.menus.service209283 = {};
            $scope.menus.service209283.update = function (node){
                $scope.menus.service209283.loading = true;
                service209283.list($scope,$dibContainer, !!node? node.id : null).then(function (menu) {
                    $scope.menus.service209283.loading = false;
                    if (!!node === false) { 
                        $scope.menus.service209283.menu = menu;
                    } else { 
                        node.children = menu;
                    }
                }, function () {
                    $scope.menus.service209283.loading = false;
                });
            };
            $scope.menus.service209283.update();
    $scope.dropdowns = {};
            $scope.openAuditTrail = function (portAlias) {
                $log.warn('Audit trail not available for this container, as a table is not linked to this view');
            };
            $scope.recordStatus = "create";
            $scope.reloadContainer = function () {
                //Now we reload all the required 
                dibContainer.reloadChildren({containerName: 'aatemp'});
            };
            if (!!dibContainer.isSubContainer($("[dib-container=aatemp]")) === false) { 
                $scope.reloadContainer();
            }
        };
        //required for permissions
        ctrl.$postLink = function () {
        };
    } 
})();
