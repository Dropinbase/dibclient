(function() {
  var require = ['/dropins/setNgMaterial/dibForm/js/component/formPaging.js','/dropins/setNgMaterial/dibGlobals/js/component/timeDatePickerInput.js' ];
  require.push('/dropins/setNgMaterial/dibGlobals/js/component/selectDefault.js');
  var files = [];
  angular.forEach(require, function (file,index) { 
    files.push(DIB__base_url+'/files'+file);
  });
    var staffSkillForm = angular.module('staffSkillForm', [files]).run(['$rootScope', 'actionLoaderService', function($rootScope, actionLoaderService) {
            var actions = ['dibGlobals.action.OpenContainer','dibGlobals.action.ToggleMenu'];
            if (!actions || actions.length === 0) {
                return;
            }
            actionLoaderService.loadDibActionDefinitions(actions);
    }]);
    staffSkillForm.factory('service195636',['ajax', 'discoverService', '$q', function (ajax, discoverService, $q) {
            return {
                list : list
            };       
            function list(query, modelType, page) {
                var url = DIB__base_url + "/peff/Crud/componentlist/staffSkillForm?containerItemId=195636";
                var $defer = $q.defer();
               if (!!page === false) {
                   url += "&page=1&limit=20";
               } else {
                   url += "&limit=20&page="+ page;
               }
               if (modelType=='search') { 
                   url += "&showUsedOnly=true";
               }
               if (!!query === false) { 
                   url += "&query=";
               } else {
                   url += "&query="+query;
               }
                var requestInfo = discoverService.addToUrl({
                    field :$('#ci195636'),
                    container : $('[dib-container=staffSkillForm]'),
                    url : url
                });
               return ajax.post(requestInfo, {
                    excludeFromCancels : true,
                    factoryResult : function (data) {
                        return data.records;
                    }
                });
            };
    }]);
    staffSkillForm.factory('service195637',['ajax', 'discoverService', '$q', function (ajax, discoverService, $q) {
            return {
                list : list
            };       
            function list(query, modelType, page) {
                var url = DIB__base_url + "/peff/Crud/componentlist/staffSkillForm?containerItemId=195637";
                var $defer = $q.defer();
               if (!!page === false) {
                   url += "&page=1&limit=20";
               } else {
                   url += "&limit=20&page="+ page;
               }
               if (modelType=='search') { 
                   url += "&showUsedOnly=true";
               }
               if (!!query === false) { 
                   url += "&query=";
               } else {
                   url += "&query="+query;
               }
                var requestInfo = discoverService.addToUrl({
                    field :$('#ci195637'),
                    container : $('[dib-container=staffSkillForm]'),
                    url : url
                });
               return ajax.post(requestInfo, {
                    excludeFromCancels : true,
                    factoryResult : function (data) {
                        return data.records;
                    }
                });
            };
    }]);
var staffSkillForm = angular.module('staffSkillForm');
staffSkillForm.component('staffSkillForm', {
    controllerAs: 'staffSkillForm',
    templateUrl: DIB__base_url + '/dropins/setNgMaterial/dibForm/Template/view/staffSkillForm.html',
    controller : component__staffSkillForm
});
component__staffSkillForm.$inject = [  
            '$scope', '$ocLazyLoad', '$rootScope', '$log', 'discoverService', 'messageService','$location','actionService', '$q','utilityService', '$timeout', 'activeFilterOverride','service195636','service195637','containerService','recordFactory','crudService','utilityService'];
    function component__staffSkillForm($scope, $ocLazyLoad, $rootScope, $log, discoverService, messageService,$location,actionService, $q,utilityService, $timeout, activeFilterOverride,service195636,service195637,containerService,recordFactory,crudService,utilityService) {
        var vm = this;        
        vm.$onInit = function () {
            $("[dib-container=staffSkillForm]").trigger('onInit');
                        $scope.container = {};
            $scope.container.Id = '8614';
            $scope.container.Name = 'staffSkillForm';
            $("[dib-container='staffSkillForm']").trigger('load');
            var crudController = "/peff/Crud";
                    $scope.model = {};
        $scope.model._GET = recordFactory.modelConstruct();
        $scope.model._GET['id'] = containerService.modelItem("number", 
            false,
            false,
            null
        );
        $scope.model._GET['staff_id'] = containerService.modelItem("select", 
            true,
            true,
            service195636
        );
        $scope.model._GET['skill_id'] = containerService.modelItem("select", 
            true,
            true,
            service195637
        );
        $scope.model._GET['rating'] = containerService.modelItem("boolean", 
            true,
            true,
            null
        );
        $scope.model._GET['yrs_of_exp'] = containerService.modelItem("number", 
            true,
            true,
            null
        );
        $scope.model._GET['self_comment'] = containerService.modelItem("text", 
            true,
            true,
            null
        );
        $scope.model._GET['manager_comment'] = containerService.modelItem("text", 
            true,
            true,
            null
        );
        $scope.model._GET['file1'] = containerService.modelItem("text", 
            true,
            true,
            null
        );
        $scope.model._GET['file2'] = containerService.modelItem("text", 
            true,
            true,
            null
        );
        $scope.model._GET['updated'] = containerService.modelItem("dateTime", 
            true,
            true,
            null
        );
        $scope.view = {};
            var reloadContainerFunctions = [];
            containerService.watchModel($scope);
	        $scope.common = $rootScope.common;
            $scope.primaryKeys = ["id"];
        $scope.menus = {};
        $scope.dropdowns = {};
        //load all dropdowns required for this container
            $scope.dropdowns.service195636 = {};
            $scope.dropdowns.service195636.items = [];
            var setTimeoutCancel195636 = null;
            var query195636;
            $scope.loadService195636 = function (query, skipTimeout) {
                //This is to prevent to load any unnessary data as this gets triggered a few times with the multi line editing
                if (query195636 != query) {
                    query195636 = query;
                    if (!!skipTimeout) {
                        return service195636.list(query).then(function (items) {
                            $scope.dropdowns.service195636.items = items;
                        });
                    } else {
                        clearTimeout(setTimeoutCancel195636);
                        setTimeoutCancel195636 = setTimeout(function () { 
                            service195636.list(query).then(function (items) {
                                $scope.dropdowns.service195636.items = items;
                            });
                        }, 100);
                    }
                } 
            };
            $scope.dropdowns.service195636.searchTextChange = function (query) {
                $scope.loadService195636(query);
            };
            $scope.dropdowns.service195636.getItems = function (query,modelType) {
                //return promise this will asign the result to the items
                return service195636.list(query,modelType);
            };
            $scope.select195636 = function (model,modelName,fieldName) {
                if (!!modelName == false) modelName ="model";
                var $element =  $('#ci195636');
                if (!!$element.attr('select-model') === false) { 
                    $element = $element.find('[select-model]');
                }
                var modelName =  'selected___' + fieldName;
                $element.trigger('select');
                if (!!model[modelName]  === false) { 
                    return  false;
                }
                angular.forEach($scope.dropdowns.service195636.items, function (item, index)   {
                    if (item.id == model[modelName]['id']) {
                        model[modelName]['id_display_value'] = item.id_display_value; 
                        return false;
                    }
                });
            };
            var setTimeout195636 = null;
            //loadService195636();
            //reloadContainerFunctions.push($scope.loadService195636);
            $scope.dropdowns.service195637 = {};
            $scope.dropdowns.service195637.items = [];
            var setTimeoutCancel195637 = null;
            var query195637;
            $scope.loadService195637 = function (query, skipTimeout) {
                //This is to prevent to load any unnessary data as this gets triggered a few times with the multi line editing
                if (query195637 != query) {
                    query195637 = query;
                    if (!!skipTimeout) {
                        return service195637.list(query).then(function (items) {
                            $scope.dropdowns.service195637.items = items;
                        });
                    } else {
                        clearTimeout(setTimeoutCancel195637);
                        setTimeoutCancel195637 = setTimeout(function () { 
                            service195637.list(query).then(function (items) {
                                $scope.dropdowns.service195637.items = items;
                            });
                        }, 100);
                    }
                } 
            };
            $scope.dropdowns.service195637.searchTextChange = function (query) {
                $scope.loadService195637(query);
            };
            $scope.dropdowns.service195637.getItems = function (query,modelType) {
                //return promise this will asign the result to the items
                return service195637.list(query,modelType);
            };
            $scope.select195637 = function (model,modelName,fieldName) {
                if (!!modelName == false) modelName ="model";
                var $element =  $('#ci195637');
                if (!!$element.attr('select-model') === false) { 
                    $element = $element.find('[select-model]');
                }
                var modelName =  'selected___' + fieldName;
                $element.trigger('select');
                if (!!model[modelName]  === false) { 
                    return  false;
                }
                angular.forEach($scope.dropdowns.service195637.items, function (item, index)   {
                    if (item.id == model[modelName]['id']) {
                        model[modelName]['id_display_value'] = item.id_display_value; 
                        return false;
                    }
                });
            };
            var setTimeout195637 = null;
            //loadService195637();
            //reloadContainerFunctions.push($scope.loadService195637);
            $scope.openAuditTrail = function (portAlias) {
                            var primaryKeyData = recordFactory.primaryKeyAuditIds($scope.primaryKeys, $scope.model);
                            if (primaryKeyData.length > 0) {
                            $scope.common.goTo(DIB__audit_trail_container,portAlias,true,"?filter_pef_table_id=4247&filter_record_id="+primaryKeyData);
                        } else {
                            $scope.common.goTo(DIB__audit_trail_container,portAlias,true,"?filter_pef_table_id=4247");
                        }
            };
            /**
            * Add a new record 
            */
            $scope.addModel = function (options) { 
                if ($scope.loadingData) return;
                var isUrlContainer = containerService.urlContainer("staffSkillForm");
                if (isUrlContainer){
                    utilityService.updateUrlPrimaryKeys($scope, true, {});
                    $location.search("record","new");
                } 
                $scope.clearForm();  
                activeFilterOverride.remove = true;
                $scope.recordStatus = "create";
            };
            /**
            * Delete the record from the database
            */
            $scope.deleteModel = function (options) { 
                if ($scope.loadingData) return;
                if ($scope.recordStatus == "create") {
                    messageService.callout({
                        message : 'No valid record to delete.',
                        type: "notice"
                    });
                    return;
                }
                messageService.confirm({
                    title: 'Delete record',
                    text : 'Warning - are you sure you want to permanently delete the current records?'
                }).then(function () { 
                    $scope.recordStatus = null;
                    var primaryKeyData = recordFactory.primaryKeyData($scope.primaryKeys, $scope.model);
                    return crudService.delete(crudController,'staffSkillForm',{
                            primaryKeyData : oldPrimaryKeyData
                    }).then(function (model) { 
                        $scope.loadingData =  false;
                        $("[dib-container=staffSkillForm]").trigger('delete');
                        $scope.staffSkillForm.$setUntouched();
                        $scope.staffSkillForm.$setPristine();
                        messageService.callout({
                            message : "Record successfully deleted.",
                            type : 'success'
                        });
                        var isUrlContainer = containerService.urlContainer('staffSkillForm');
                        if (isUrlContainer) {
                            utilityService.updateUrlPrimaryKeys($scope, options.redirect, model);
                        } else {
                            $scope.addModel();
                        }
                    }, function (data) { 
                        $scope.loadingData =  false;
                    });
                });
            };         
            /**
            *  Saving record in this model
            */
            $scope.saveModel = function (options) {
                if ($scope.loadingData) return;
                if (!$scope.staffSkillForm.$valid) { 
                    $scope.recordStatus = null;
                    containerService.makeFormDirty($scope.staffSkillForm);
                    var errorMsg = "";
                    angular.forEach($scope.staffSkillForm.$error, function (errorCategory,index) { 
                        angular.forEach(errorCategory, function (error, key) { 
                            errorMsg += index + ":"+error.$name + "; ";
                        });
                    });
                    var msg = 'Resolve validation errors to save on fields: '+errorMsg + '';
                    messageService.callout({
                        message : msg,
                        type : 'warning',
                        duration : 10000
                    });
                    return $q.reject(msg);
                } 
                $scope.loadingData =  true;
                var primaryKeyData = recordFactory.primaryKeyData($scope.primaryKeys, $scope.model);
                $("[dib-container=staffSkillForm]").trigger('beforeUpdate');
                return crudService.update(crudController,'staffSkillForm',{
                            primaryKeyData : oldPrimaryKeyData
            }, $scope).then(function (model) {
                    oldPrimaryKeyData = primaryKeyData;
                    $scope.loadingData =  false;
                    $scope.staffSkillForm.$setUntouched();
                    $scope.staffSkillForm.$setPristine();
                    $rootScope._navigation.reset();
                    if (Object.keys(primaryKeyData).length>0) {
                        $("[dib-container=staffSkillForm]").trigger('update');
                        messageService.callout({
                            message : "Record successfully saved.",
                            type : 'success'
                        });
                        return $q.reject($scope);
                    } else {
                        $("[dib-container=staffSkillForm]").trigger('create');
                        messageService.callout({
                            message : "Record successfully created.",
                            type : 'success'
                        });
                        var isUrlContainer = containerService.urlContainer('staffSkillForm');
                        if (isUrlContainer) {
                            utilityService.updateUrlPrimaryKeys($scope,  options.redirect, model);
                        } else {
                            //if primary key data is empty then we assume a new record has been created and store the model received against the primaryKeyData
                            $scope.primaryKeyData = model;
                            oldPrimaryKeyData = $scope.primaryKeyData;
                            activeFilterOverride.remove = true;
                            loadForm();
                        }
                        return $q.resolve($scope);
                    }
                }, function () {
                    $scope.loadingData =  false;
                    return $q.reject($scope);
                });
            };
            var mdTabs = $("[dib-container=staffSkillForm]").closest('md-tabs');
            var tabsScope = null;
            if (mdTabs.length === 0) {
                mdTabs = null;
            }
            else {
                tabsScope = mdTabs.scope();
            }
            $scope.$watch('staffSkillForm.$dirty', function (newValue, oldValue) { 
                $rootScope._navigation.containerDirty = newValue;
                if ($rootScope._navigation.containerDirty && !!mdTabs && !!tabsScope) {
                    var currentIndex = tabsScope.selectedIndex;
                    $rootScope._navigation.addSaveFailedPromise(function(tabIndex) {
                        tabsScope.selectedIndex = tabIndex;
                        return $q.when();
                    }, [currentIndex]);
                }
            },true);
            $rootScope._navigation.addSavePromise($scope.saveModel);
            //Giving the form the ability to handle dependency data (if an event is trigger on another component that has a refresh item alias point to the form/child form)
            $scope.setDependencyData = function (object) { 
                    $scope.primaryKeyData = recordFactory.primaryKeyData($scope.primaryKeys, object);
                    return $q.when();
            };
            $scope.itemAliasData = function () {
                return angular.copy($scope.primaryKeyData);
            };
            /**
            * Clear the form from records and dependancy data
            */
            $scope.clearForm = function () {
                if ($scope.loadingData) return;
                $scope.primaryKeyData = null;
                $scope.reloadContainer();
                containerService.cleanForm($scope.staffSkillForm,$scope.model);
                $("[dib-container=staffSkillForm]").trigger('clearDependency');
            };
            var oldPrimaryKeyData;
            function loadForm () { 
                $("[dib-container=staffSkillForm]").trigger('beforeload');
                //clear the record status
                $scope.recordStatus = null;
                var isUrlContainer = containerService.urlContainer('staffSkillForm');
                if (isUrlContainer) {
                    //check if record is first or last
                    if (!!$location.search()['record'] && $location.search()['record']!='new') {
                        return;
                    }
                }
                //check if this is a child container 
                $scope.primaryKeyData = isUrlContainer ? discoverService.getPrimaryKeysFromUrl() : $scope.primaryKeyData;
                if (!!$scope.primaryKeyData === false) {
                $scope.primaryKeyData = {};
                //set all the primaryKeys to 0
                angular.forEach($scope.primaryKeys, function (key,index) { 
                    $scope.model[key] = 0;
                });
                }
                //remove active filter if no record are available
                if (Object.keys($scope.primaryKeyData).length ==0) {
                    $scope.recordStatus = "create";
                } else {
                    $scope.recordStatus = "update";
                }
                $scope.loadingData = true;
                oldPrimaryKeyData = $scope.primaryKeyData;
                crudService.read(crudController,'staffSkillForm',{
                    primaryKeyData : oldPrimaryKeyData,
                    createParams: "{}"
                },
                $scope
                ).then(function (model) {
                    $scope.model = model;
                    oldPrimaryKeyData = recordFactory.primaryKeyData($scope.primaryKeys, $scope.model);
                    $("[dib-container=staffSkillForm]").trigger('load');
                    $scope.staffSkillForm.$setUntouched();
                    $scope.staffSkillForm.$setPristine();
                    //@TODO refactor start 
                    var timeoutTest = $timeout(function () { 
                        containerService.reloadChildren({containerName: 'staffSkillForm'});
                    },500); 
                    //@FRANCOIS @TODO Maybe we can change this to a better magic number :)
                    var eventDeregister = $rootScope.$on("dibAngular.subcontainerLoaded", function () {
                        $timeout.cancel(timeoutTest);
                        timeoutTest = $timeout(function () { 
                            containerService.reloadChildren({containerName: 'staffSkillForm'});
                            eventDeregister();
                        },500); 
                        //@FRANCOIS @TODO Maybe we can change this to a better magic number :)
                    });
                    //Refactor stop
                    $log.debug("containerName read->model",$scope.model );
                    $scope.loadingData =  false;
                }, function () {
                    $scope.loadingData =  false;
                });
            }
            $scope.reloadContainer = function (options) {
                $("[dib-container=staffSkillForm]").trigger('beforeReloadContainer');
                $scope.autoRefresh = true;
                loadForm ();
            };
            //var isUrlContainer = containerService.urlContainer('staffSkillForm');
            //the if below we removed the requirement to match the url to the containerName, so we can load data into a window without the url changing. wizBuildAppAdv.
            loadForm ();
            $scope.$watch('model', function (oldVal, newVal) {
                $("[dib-container=staffSkillForm]").trigger('modelChange');
            },true);  
        }
    }
})();
