(function() {
  var require = ['/dropins/setNgMaterial/dibMenu/js/components/dibSideBarStatus.js' ];
  require.push('/dropins/setNgMaterial/dibGlobals/js/component/selectDefault.js');
  var files = [];
  angular.forEach(require, function (file,index) { 
    files.push(enviroment_base_url+'/files'+file);
  });
    var dibdemo13 = angular.module('dibdemo13', [files]).run(['$rootScope', 'actionLoaderService', function($rootScope, actionLoaderService) {
            var actions = ['dibGlobals.action.OpenContainer','dibGlobals.action.ToggleMenu'];
            if (!actions || actions.length === 0) {
                return;
            }
            actionLoaderService.loadDibActionDefinitions(actions);
    }]);
    dibdemo13.factory('service181675',['ajax', '$log', 'discoverService', function (ajax, $log, discoverService) {
            var list = function (node) {
               var url = enviroment_base_url + "/dropins/setNgMaterial/dibMenu/menu/list.json?containerName=dibdemo13";
               if (!!node === false) {
                   url += "&node=root&rootMenuId=181676";
               } else {
                   url += "&node="+node+"&rootMenuId=181676";
               }
                var requestInfo = discoverService.addToUrl({
                    field :$('#ci181675'),
                    container : $('[dib-container=dibdemo13]'),
                    url : url
                });
                return ajax.post(requestInfo, {
                    excludeFromCancels : true,
                    factoryResult : function (data) {
                        return data.records;
                    }
                })
            };
            return {
                list : list
            };
    }]); 
})();
(function() {
var dibdemo13 = angular.module('dibdemo13');
dibdemo13.component('dibdemo13', {
    controllerAs: 'dibdemo13',
    templateUrl: enviroment_base_url + '/dropins/setNgMaterial/dibContainer/Template/view/dibdemo13.html',
    controller : component__dibdemo13
});
component__dibdemo13.$inject = [  
            '$scope', '$ocLazyLoad', '$rootScope', '$log', '$q','service181675','containerService','recordFactory'];
    function component__dibdemo13($scope, $ocLazyLoad, $rootScope, $log, $q,service181675,containerService,recordFactory) {
                     $scope.container = {};
            $scope.container.Id = '8251';
            $scope.container.Name = 'dibdemo13';
            $("[dib-container='dibdemo13']").trigger('load');
                    $scope.model = {};
        $scope.model._GET = recordFactory.modelConstruct();
            var reloadContainerFunctions = [];
            containerService.watchModel($scope);
	        $scope.common = $rootScope.common;
            $scope.primaryKeys = [];
        $scope.menus = {};
            $scope.menus.service181675 = {};
            $scope.menus.service181675.update = function (node){
                $scope.menus.service181675.loading = true;
                service181675.list(!!node? node.id : null).then(function (menu) {
                    $scope.menus.service181675.loading = false;
                    if (!!node === false) { 
                        $scope.menus.service181675.menu = menu;
                    } else { 
                        node.children = menu;
                    }
                }, function () {
                    $scope.menus.service181675.loading = false;
                });
            };
            $scope.menus.service181675.update();
        $scope.dropdowns = {};
        //load all dropdowns required for this container
            $scope.openAuditTrail = function (portAlias) {
                $log.warn('Audit trail not available for this container, as a table is not linked to this view');
            };
        $scope.reloadContainer = function () {
            //Now we reload all the required 
            containerService.reloadChildren({containerName: 'dibdemo13'});
        };
        if (!!containerService.isSubContainer($("[dib-container=dibdemo13]")) === false) { 
            $scope.reloadContainer();
        }
    } 
})();
