(function() {
  var require = ['/dropins/setNgMaterial/dibGlobals/js/component/timeDatePickerInput.js' ];
  require.push('/dropins/setNgMaterial/dibGlobals/js/component/selectDefault.js');
  var files = [];
  angular.forEach(require, function (file,index) { 
    files.push(DIB__base_url+'/files'+file);
  });
    var individualForm = angular.module('individualForm', [files]).run(['$rootScope', 'actionLoaderService', function($rootScope, actionLoaderService) {
            var actions = ['dibGlobals.action.OpenContainer','dibGlobals.action.ToggleMenu'];
            if (!actions || actions.length === 0) {
                return;
            }
            actionLoaderService.loadDibActionDefinitions(actions);
    }]);
var individualForm = angular.module('individualForm');
individualForm.component('individualForm', {
    controllerAs: 'individualForm',
    templateUrl: DIB__base_url + '/dropins/setNgMaterial/dibForm/Template/view/individualForm.html',
    controller : component__individualForm
});
component__individualForm.$inject = [  
            '$scope', '$ocLazyLoad', '$rootScope', '$log', 'discoverService', 'messageService','$location','actionService', '$q','utilityService', '$timeout', 'activeFilterOverride','containerService','recordFactory','crudService','utilityService'];
    function component__individualForm($scope, $ocLazyLoad, $rootScope, $log, discoverService, messageService,$location,actionService, $q,utilityService, $timeout, activeFilterOverride,containerService,recordFactory,crudService,utilityService) {
        var vm = this;        
        vm.$onInit = function () {
                        $scope.container = {};
            $scope.container.Id = '8524';
            $scope.container.Name = 'individualForm';
            $("[dib-container='individualForm']").trigger('load');
            var crudController = "/peff/Crud";
                    $scope.model = {};
        $scope.model._GET = recordFactory.modelConstruct();
        $scope.model._GET['identification_type'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['id_passport_no'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['first_name'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['last_name'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['race'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['gender'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['title'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['previous_id_no'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['nationality_id'] = containerService.modelItem("number", 
            false,
            false
        );
        $scope.model._GET['language_id'] = containerService.modelItem("number", 
            false,
            false
        );
        $scope.model._GET['date_of_birth'] = containerService.modelItem("date", 
            false,
            false
        );
        $scope.model._GET['disability_id'] = containerService.modelItem("number", 
            false,
            false
        );
        $scope.model._GET['email'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['phone_number'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['mobile'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['fax_number'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['newsletter'] = containerService.modelItem("boolean", 
            false,
            false
        );
        $scope.model._GET['role_id'] = containerService.modelItem("number", 
            false,
            false
        );
        $scope.model._GET['industry_id'] = containerService.modelItem("number", 
            false,
            false
        );
        $scope.model._GET['job_title'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['occupational_category_id'] = containerService.modelItem("number", 
            false,
            false
        );
        $scope.model._GET['main_qualification_id'] = containerService.modelItem("number", 
            false,
            false
        );
        $scope.model._GET['company_branch'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['company_employee_number'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['company_costcentre'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['employee_group'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['employer_name'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['employer_physical_address'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['province'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['fsp_number'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['postal_code'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['fsp_name'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.model._GET['exam_country_id'] = containerService.modelItem("number", 
            false,
            false
        );
        $scope.model._GET['department_or_branch'] = containerService.modelItem("text", 
            false,
            false
        );
        $scope.view = {};
            var reloadContainerFunctions = [];
            containerService.watchModel($scope);
	        $scope.common = $rootScope.common;
            $scope.primaryKeys = ["id"];
        $scope.menus = {};
        $scope.dropdowns = {};
        //load all dropdowns required for this container
            $scope.openAuditTrail = function (portAlias) {
                            var primaryKeyData = recordFactory.primaryKeyAuditIds($scope.primaryKeys, $scope.model);
                            if (primaryKeyData.length > 0) {
                            $scope.common.goTo(DIB__audit_trail_container,portAlias,true,"?filter_pef_table_id=4084&filter_record_id="+primaryKeyData);
                        } else {
                            $scope.common.goTo(DIB__audit_trail_container,portAlias,true,"?filter_pef_table_id=4084");
                        }
            };
            /**
            * Add a new record 
            */
            $scope.addModel = function (options) { 
                if ($scope.loadingData) return;
                var isUrlContainer = containerService.urlContainer("individualForm");
                if (isUrlContainer){
                    utilityService.updateUrlPrimaryKeys($scope, true, {});
                    $location.search("record","new");
                } 
                $scope.clearForm();  
                activeFilterOverride.remove = true;
                $scope.recordStatus = "create";
            };
            /**
            * Delete the record from the database
            */
            $scope.deleteModel = function (options) { 
                if ($scope.loadingData) return;
                if ($scope.recordStatus == "create") {
                    messageService.callout({
                        message : 'No valid record to delete.',
                        type: "notice"
                    });
                    return;
                }
                messageService.confirm({
                    title: 'Delete record',
                    text : 'Warning - are you sure you want to permanently delete the current records?'
                }).then(function () { 
                    $scope.recordStatus = null;
                    var primaryKeyData = recordFactory.primaryKeyData($scope.primaryKeys, $scope.model);
                    return crudService.delete(crudController,'individualForm',{
                            primaryKeyData : oldPrimaryKeyData
                    }).then(function (model) { 
                        $scope.loadingData =  false;
                        $("[dib-container=individualForm]").trigger('delete');
                        $scope.individualForm.$setUntouched();
                        $scope.individualForm.$setPristine();
                        messageService.callout({
                            message : "Record successfully deleted.",
                            type : 'success'
                        });
                        var isUrlContainer = containerService.urlContainer('individualForm');
                        if (isUrlContainer) {
                            utilityService.updateUrlPrimaryKeys($scope, options.redirect, model);
                        } else {
                            $scope.addModel();
                        }
                    }, function (data) { 
                        $scope.loadingData =  false;
                    });
                });
            };         
            /**
            *  Saving record in this model
            */
            $scope.saveModel = function (options) {
                if ($scope.loadingData) return;
                if (!$scope.individualForm.$valid) { 
                    $scope.recordStatus = null;
                    containerService.makeFormDirty($scope.individualForm);
                    var errorMsg = "";
                    angular.forEach($scope.individualForm.$error, function (errorCategory,index) { 
                        angular.forEach(errorCategory, function (error, key) { 
                            errorMsg += index + ":"+error.$name + "; ";
                        });
                    });
                    var msg = 'Resolve validation errors to save on fields: '+errorMsg + '';
                    messageService.callout({
                        message : msg,
                        type : 'warning',
                        duration : 10000
                    });
                    return $q.reject(msg);
                } 
                $scope.loadingData =  true;
                var primaryKeyData = recordFactory.primaryKeyData($scope.primaryKeys, $scope.model);
                $("[dib-container=individualForm]").trigger('beforeUpdate');
                return crudService.update(crudController,'individualForm',{
                            primaryKeyData : oldPrimaryKeyData
            }, $scope).then(function (model) {
                    oldPrimaryKeyData = primaryKeyData;
                    $scope.loadingData =  false;
                    $scope.individualForm.$setUntouched();
                    $scope.individualForm.$setPristine();
                    $rootScope._navigation.reset();
                    if (Object.keys(primaryKeyData).length>0) {
                        $("[dib-container=individualForm]").trigger('update');
                        messageService.callout({
                            message : "Record successfully saved.",
                            type : 'success'
                        });
                        return $q.reject($scope);
                    } else {
                        $("[dib-container=individualForm]").trigger('create');
                        messageService.callout({
                            message : "Record successfully created.",
                            type : 'success'
                        });
                        var isUrlContainer = containerService.urlContainer('individualForm');
                        if (isUrlContainer) {
                            utilityService.updateUrlPrimaryKeys($scope,  options.redirect, model);
                        } else {
                            //if primary key data is empty then we assume a new record has been created and store the model received against the primaryKeyData
                            $scope.primaryKeyData = model;
                            oldPrimaryKeyData = $scope.primaryKeyData;
                            activeFilterOverride.remove = true;
                            loadForm();
                        }
                        return $q.resolve($scope);
                    }
                }, function () {
                    $scope.loadingData =  false;
                    return $q.reject($scope);
                });
            };
            var mdTabs = $("[dib-container=individualForm]").closest('md-tabs');
            var tabsScope = null;
            if (mdTabs.length === 0) {
                mdTabs = null;
            }
            else {
                tabsScope = mdTabs.scope();
            }
            $scope.$watch('individualForm.$dirty', function (newValue, oldValue) { 
                $rootScope._navigation.containerDirty = newValue;
                if ($rootScope._navigation.containerDirty && !!mdTabs && !!tabsScope) {
                    var currentIndex = tabsScope.selectedIndex;
                    $rootScope._navigation.addSaveFailedPromise(function(tabIndex) {
                        tabsScope.selectedIndex = tabIndex;
                        return $q.when();
                    }, [currentIndex]);
                }
            },true);
            $rootScope._navigation.addSavePromise($scope.saveModel);
            //Giving the form the ability to handle dependency data (if an event is trigger on another component that has a refresh item alias point to the form/child form)
            $scope.setDependencyData = function (object) { 
                    $scope.primaryKeyData = recordFactory.primaryKeyData($scope.primaryKeys, object);
                    return $q.when();
            };
            /**
            * Clear the form from records and dependancy data
            */
            $scope.clearForm = function () {
                if ($scope.loadingData) return;
                $scope.primaryKeyData = null;
                $scope.reloadContainer();
                containerService.cleanForm($scope.individualForm,$scope.model);
                $("[dib-container=individualForm]").trigger('clearDependency');
            };
            var oldPrimaryKeyData;
            function loadForm () { 
                $("[dib-container=individualForm]").trigger('beforeload');
                //clear the record status
                $scope.recordStatus = null;
                var isUrlContainer = containerService.urlContainer('individualForm');
                if (isUrlContainer) {
                    //check if record is first or last
                    if (!!$location.search()['record'] && $location.search()['record']!='new') {
                        return;
                    }
                }
                //check if this is a child container 
                $scope.primaryKeyData = isUrlContainer ? discoverService.getPrimaryKeysFromUrl() : $scope.primaryKeyData;
                if (!!$scope.primaryKeyData === false) {
                $scope.primaryKeyData = {};
                //set all the primaryKeys to 0
                angular.forEach($scope.primaryKeys, function (key,index) { 
                    $scope.model[key] = 0;
                });
                }
                //remove active filter if no record are available
                if (Object.keys($scope.primaryKeyData).length ==0) {
                    $scope.recordStatus = "create";
                } else {
                    $scope.recordStatus = "update";
                }
                $scope.loadingData = true;
                oldPrimaryKeyData = $scope.primaryKeyData;
                crudService.read(crudController,'individualForm',{
                    primaryKeyData : oldPrimaryKeyData,
                    createParams: "{}"
                },
                $scope
                ).then(function (model) {
                    $scope.model = model;
                    $("[dib-container=individualForm]").trigger('load');
                    $scope.individualForm.$setUntouched();
                    $scope.individualForm.$setPristine();
                    //@TODO refactor start 
                    var timeoutTest = $timeout(function () { 
                        containerService.reloadChildren({containerName: 'individualForm'});
                    },500); 
                    //@FRANCOIS @TODO Maybe we can change this to a better magic number :)
                    var eventDeregister = $rootScope.$on("dibAngular.subcontainerLoaded", function () {
                        $timeout.cancel(timeoutTest);
                        timeoutTest = $timeout(function () { 
                            containerService.reloadChildren({containerName: 'individualForm'});
                            eventDeregister();
                        },500); 
                        //@FRANCOIS @TODO Maybe we can change this to a better magic number :)
                    });
                    //Refactor stop
                    $log.debug("containerName read->model",$scope.model );
                    $scope.loadingData =  false;
                }, function () {
                    $scope.loadingData =  false;
                });
            }
            $scope.reloadContainer = function (options) {
                $("[dib-container=individualForm]").trigger('beforeReloadContainer');
                $scope.autoRefresh = true;
                loadForm ();
            };
            //var isUrlContainer = containerService.urlContainer('individualForm');
            //the if below we removed the requirement to match the url to the containerName, so we can load data into a window without the url changing. wizBuildAppAdv.
            loadForm ();
            $scope.$watch('model', function (oldVal, newVal) {
                $("[dib-container=individualForm]").trigger('modelChange');
            },true);  
        }
    }
})();
