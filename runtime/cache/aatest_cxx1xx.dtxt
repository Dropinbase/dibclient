
(function() {
    var require = ['/dropins/setNgMaterial/dibMenu/js/components/dibSideBarStatus.js' ];
    require.push('/dropins/setNgMaterial/dibGlobals/js/component/selectDefault.js');
    var files = [];
    angular.forEach(require, function (file,index) { 
        files.push(DIB.base_url+'/files'+file);
    });
    var aatest = angular.module('aatest', [files]);
    aatest.service('service209302',service209302);
    service209302.$inject = ['dibAjax', 'dibDiscover'];
    function service209302(dibAjax,  dibDiscover) {
            return {
                list : list
            };
            function list($scope, $element, node) {
                var url = dibDiscover.updateQueryStringMenu(DIB.base_url + "/dropins/setNgMaterial/dibMenu/menu/list.json?containerName=aatest&rootMenuId=209303", node);
                var requestInfo = dibDiscover.addToUrl({
                    field : $('#ci209302'),
                    $scope : $scope,
                    container : $element,
                    url : url
                });
                return dibAjax.post(requestInfo, {
                    excludeFromCancels : true,
                    factoryResult : function (data) {
                        return data.records;
                    }
                })
            };
    };
    var aatest = angular.module('aatest');
    aatest.component('aatest', {
        controllerAs: 'aatest',
        templateUrl: DIB.base_url + '/dropins/setNgMaterial/dibContainer/Template/view/aatest.html',
        controller : component__aatest
    });
    component__aatest.$inject = [ '$scope','$element','dibCrud','dibNavigation','dibAction','dibContainer','dibDiscover','dibRecord','dibUtility','dibCommon','$log','$q','$ocLazyLoad','service209302'];
    function component__aatest($scope,$element,dibCrud,dibNavigation,dibAction,dibContainer,dibDiscover,dibRecord,dibUtility,dibCommon,$log,$q,$ocLazyLoad,service209302) {
        var ctrl = this;
                    $scope.container = {};
            $scope.container.Id = '9075';
            $scope.container.Name = 'aatest';
            $scope.container.SubContainerItemAliases = []; 
            var crudController = "/peff/Crud";
                    $scope.model = {};
        $scope.model._GET = {};
        $scope.view = {};
            $scope.view.menu = {'disabled' : false,'visible' : true, 'class' : null, 'style' : null };
            $scope.view.menuItems = {'disabled' : false,'visible' : true, 'class' : null, 'style' : null };
            var reloadContainerFunctions = [];
            dibContainer.watchModel($scope);
	        $scope.dibCommon = dibCommon;
            $scope.primaryKeys = [];
        var $dibContainer = $element.first();
        $scope.menus = {};
            $scope.menus.service209302 = {};
            $scope.menus.service209302.update = function (node){
                $scope.menus.service209302.loading = true;
                service209302.list($scope,$dibContainer, !!node? node.id : null).then(function (menu) {
                    $scope.menus.service209302.loading = false;
                    if (!!node === false) { 
                        $scope.menus.service209302.menu = menu;
                    } else { 
                        node.children = menu;
                    }
                }, function () {
                    $scope.menus.service209302.loading = false;
                });
            };
            $scope.menus.service209302.update();
    $scope.dropdowns = {};
            $scope.openAuditTrail = function (portAlias) {
                $log.warn('Audit trail not available for this container, as a table is not linked to this view');
            };
        ctrl.$onInit = function () { 
            $scope.recordStatus = "create";
            $scope.reloadContainer = function () {
                //Now we reload all the required 
                dibContainer.reloadChildren({containerName: 'aatest'});
            };
        };
        //required for permissions
        ctrl.$postLink = function () {
                $scope.reloadContainer({ force : true });
        };
    } 
})();
